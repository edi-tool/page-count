{
  "name": "page-count",
  "private": true,
  "description": "台割りに合うページ数の提案と背幅の概算を行うブラウザツール",
  "scripts": {
    "test": "node --test tests/*.test.js",
    "check": "node scripts/check-static.mjs"
  },
  "engines": {
    "node": ">=22"
  }
}
